import { dynamicAttr } from '../../../helpers/dynamic-attr';
import { module, test } from 'qunit';

module('Unit | Helper | dynamic attr');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dynamicAttr(42);
  assert.ok(result);
});

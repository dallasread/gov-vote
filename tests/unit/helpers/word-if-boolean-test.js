import { wordIfBoolean } from '../../../helpers/word-if-boolean';
import { module, test } from 'qunit';

module('Unit | Helper | word if boolean');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = wordIfBoolean(42);
  assert.ok(result);
});

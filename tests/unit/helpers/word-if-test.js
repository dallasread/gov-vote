import { isChecked } from '../../../helpers/is-checked';
import { module, test } from 'qunit';

module('Unit | Helper | is checked');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isChecked(42);
  assert.ok(result);
});
